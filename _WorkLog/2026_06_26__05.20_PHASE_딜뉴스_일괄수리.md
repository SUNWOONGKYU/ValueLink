# 딜뉴스 일괄 수리 (수집 중단 + 추출 노이즈)

**PO 지시:** "딜뉴스 수리 전체를 일괄로 진행해" (2026-06-26)
**진단 요약:**
- 자동수집 06-15 이후 중단 (스케줄러 ValueLink-DealNews LastResult=255, scheduler.log도 06-15 08:37 이후 미기록 → bat 본문 미실행)
- 수집기 자체는 수동 정상(EXIT 0)이나 추출 노이즈 심각(대학·대기업·투자사·기사조각)
- deals.created_at DEFAULT 없음 → 신규행 NULL

**작업 순서 원칙:** 필터를 먼저 고쳐야 백필/재가동 시 오염 안 됨.

## Phase 체크리스트
- [x] Phase 1: 추출기 노이즈 필터 강화 — 대학약칭('대'접미)/대기업set/투자사·펀드·VC·美/언론/기사조각(겨냥·인정·가속·잠재력)/조사(에서·으로) 거부 추가. 테스트 136/136 PASS (MUST_REJECT 46, REGRESSION 68 회귀무손실)
- [x] Phase 2: deals.created_at DEFAULT now() 적용 (Management API, column_default=now() 검증)
- [x] Phase 3: 클린 백필 — 깊은 --since는 공공기관/운용사 노이즈 과다 → 얕은 --days 12 사용. DRY 17건 중 16건 실딜 확인 → 실저장 13건(전부 실딜), DB 401→414, EXIT 0. (기관/직함/운용사/인베 필터 추가, 테스트 140/140)
- [x] Phase 4: 스케줄러 255 수리 — **근본원인 발견**: run_daily.bat이 06-15경 BOM/UTF-8로 저장돼 cmd 파싱불가('이메일' is not recognized 등) → 그날부터 수집 사망(scheduler.log도 06-15 멈춤). 해결: **bat 순수 ASCII 재작성**(BOM 없음 확인) + 이메일 실패가 작업을 255로 만들지 않게 분리 + 즉시 로깅. `run_daily.bat nomail` 검증 EXIT 0, 로그 정상. (작업 LogonType=Interactive 유지 — 로그온 시 실행, 변경은 Windows 비번 필요)
- [x] Phase 5: 검증(테스트 140/140, DB 414건 최신 클린) + work_logs/Reports 기록 + 커밋·푸시
