# ValueLink 정밀 진단 및 업그레이드 계획

**작성일**: 2026-06-11
**진단 대상**: C:\ValueLink (G드라이브에서 이관 완료, 동기화 체계 가동 중)
**진단 방법**: 탐색 에이전트 3개 병렬 조사(SAL Grid 상태 / 코드베이스 / 배포·CI·테스트) + 직접 실행 검증(build, tsc, jest, Vercel CLI, 실서비스 fetch)

---

## PART 1. 정밀 진단

### 1.1 한 줄 요약

> **SAL Grid상 32/32 Task 완료(100%)이지만, 정작 서비스는 한 번도 배포에 성공한 적이 없다.**
> 공개돼 있는 것은 구버전 정적 목업(GitHub Pages)뿐이고, 진짜 Next.js 플랫폼의 유일한 Vercel 프로덕션 배포(2026-02-28)는 Error 상태로 멈춰 있다.

### 1.2 진행 상태 (SAL Grid 기준)

| Stage | Task | 상태 | Gate 승인 |
|-------|:----:|------|----------|
| S1 개발 준비 | 4/4 | 완료 | 2026-02-08 |
| S2 개발 1차 (Core) | 12/12 | 완료 | 2026-02-09 |
| S3 개발 2차 (평가 엔진) | 4/4 | 완료 | 2026-02-22 |
| S4 개발 3차 (뉴스 크롤러) | 6/6 | 완료 | 2026-02-22 |
| S5 마무리 (테스트·문서·UX) | 6/6 | 완료 | 2026-02-25 |

- 마지막 커밋: 2026-02-26 (슬라이드쇼 디자인) — **이후 약 3.5개월 동결**
- HANDOVER.md(2/9 작성, "55%·S3 대기")는 심하게 구버전 — 갱신 필요
- ⚠️ 주목: HANDOVER의 S4 계획은 "Payment & Admin"이었으나 실제 S4는 **뉴스 크롤러**로 실행됨 → **결제·관리자 기능은 미구현 상태로 남아있을 가능성 높음**

### 1.3 치명 결함 (서비스가 안 떠 있는 이유)

| # | 결함 | 근거 | 심각도 |
|---|------|------|:------:|
| 1 | **Vercel 프로덕션 배포 Error** — 원인: Vercel 환경변수 `CRON_SECRET`에 앞뒤 공백 → 빌드 즉시 중단 | `vercel inspect --logs` (2026-02-27T19:50 빌드 로그) | 🔴 |
| 2 | **로컬 빌드 실패** — `next build` 타입체크 단계 중단 | 직접 실행 (webpack 컴파일은 성공) | 🔴 |
| 3 | **타입 오류 총 269개** — ①Supabase Database 타입 미생성 → 쿼리 결과가 전부 `never` 추론 (~240개, app/api 7개 라우트 + Process 사본) ②깨진 import 1건 (`api/External/lib/integrations/enkino-verification.ts:19`) ③tsconfig `include: **/*.ts`가 레거시 `api/`·아카이브 `Process/`까지 포함 | `npx tsc --noEmit` 전수 집계 | 🔴 |
| 4 | 공개 URL은 레거시 목업 — GitHub Pages(200 OK)가 `Valuation_Company/valuation-platform/frontend/`(1월 정적 프로토타입)로 리다이렉트 | index.html + 직접 fetch | 🟡 |

### 1.4 보조 결함·부채

| # | 항목 | 내용 |
|---|------|------|
| 5 | next@14.2.16 보안 취약점 | npm 설치 시 공식 경고 (2025-12 advisory) — 패치 버전 업그레이드 필요 |
| 6 | Process ↔ root 불일치 | `lib/valuation/engines/*` 5개 파일이 Stage 원본과 크기 다름 (sync-to-root 체계 균열) |
| 7 | jest moduleNameMapper 함정 | `@/lib/valuation/*`를 Process/S3 구버전으로 매핑 → 테스트가 옛 코드를 검증할 수 있음 |
| 8 | 테스트 실체 빈약 | Jest 통합 1파일(4 케이스, 엔진만) ✅ 통과 / Playwright E2E 1파일(실행 미검증) / 커버리지 80% 목표는 미실측 |
| 9 | 미커밋 변경 75건 | M:3(vercel.json 등) D:2 ??:70 (스크래퍼 스크립트·브랜드 SVG 등) |
| 10 | DB 미생성 테이블 | evaluation_requests·project_history·balance_payments·report_delivery_requests (HANDOVER 7.2) — 해당 API 라우트는 존재하므로 런타임 오류 예정 |
| 11 | 레거시 잔재 | 루트 `api/`(53KB), `pages/`(빈), `assets/`(빈), database/ 스키마 v1~v4 혼재 |
| 12 | 뉴스 스크래퍼 | GitHub Actions 매일 실행 설정이나 Gemini 무료 할당량 소진 이력 — 실제 가동 여부 GitHub Actions 탭 확인 필요 (gh CLI 미인증으로 이번 진단에서 확인 불가) |

### 1.5 건강한 부분

- 코드 자산 실재: 페이지 40개 + API 라우트 9개 + 평가 엔진 5종 + 워크플로/AI/이메일 모듈 (lib 20모듈)
- 시크릿 하드코딩 0건, .env 관리 양호 (9개 키)
- Git: origin(GitHub) 완전 동기화, pre-commit 자동화 체계 존재
- CI/CD 워크플로 5개 구성 완료 (정상 작동 여부는 별개)
- Jest 통과 4/4, Supabase DB에 실데이터 존재 (뉴스 1,003건, 딜 140건 등)

---

## PART 2. 업그레이드 계획 (PO 승인 대상)

> 실행은 SAL Grid 규정에 따름: 신규 Stage(S6 제안) 등록 → MBO 제시 → PO 승인 → Task 실행.
> 아래는 계획이며, 승인 전 코드 수정·Task 등록을 하지 않는다.

### Track A — 긴급 정상화: "사이트를 실제로 띄운다" 🔴

| # | 작업 | 내용 | 검증 기준 |
|---|------|------|----------|
| A1 | Vercel CRON_SECRET 재설정 | 대시보드(또는 vercel env)에서 공백 제거 후 재저장 | vercel build 통과 |
| A2 | 깨진 import + tsconfig 수정 | enkino import → `@/lib/valuation/engines/dcf-engine`, exclude에 `Process`, `api` 추가 | 해당 오류 소멸 |
| A3 | Supabase Database 타입 생성 | `supabase gen types typescript` → `types/database.ts`, 클라이언트에 제네릭 적용 | tsc 오류 269 → 0 |
| A4 | DB 미생성 테이블 4개 생성 | schema-v4-final.sql 기준 (파괴적 작업 아님 — CREATE만) | 테이블 존재 + API 200 |
| A5 | 로컬 빌드 → Vercel 프로덕션 재배포 | `next build` 통과 후 push → cd.yml 또는 vercel deploy | 배포 Status Ready |
| A6 | **브라우저 클릭 검증** (UI 검증 철칙) | 배포 URL에서 주요 버튼·링크 전수 클릭 — 별도 Verification Agent(Playwright) 수행, curl 200으로 Verified 처리 금지 | 사용자 여정 KPI 통과 |
| A7 | 미커밋 75건 정리 커밋 | 분류 후 커밋/무시 결정, push | working tree clean |

### Track B — 기반 정비: "다시 안 무너지게" 🟡

| # | 작업 | 내용 |
|---|------|------|
| B1 | Next.js 보안 패치 업그레이드 | 14.2.16 → 14.x 최신 패치 (메이저 업그레이드는 별도 결정) |
| B2 | 레거시 정리 | 루트 `api/`·`pages/`·`assets/` 정리, database/ → migrations 체계화, GitHub Pages 용도 재정의(목업 유지 vs 폐기) |
| B3 | 테스트 정합화 | jest moduleNameMapper 수정(root 기준), Process↔root 동기화 복구, 커버리지 실측, E2E 실행 검증 |
| B4 | 문서 갱신 | HANDOVER.md 현행화, 배포 런북 작성 |

### Track C — 기능 업그레이드 (S6 Stage 후보) 🟢

PO 결정 필요 — 대안 3개 + 추천:

| 옵션 | 내용 | 추천 |
|------|------|:----:|
| C-1. **실사용 가능화 우선** | AI 기능 실연동 검증(S2BA4 미테스트 이력), 회원가입→평가신청→결과 전 여정 실동작, 도메인 연결 | ⭐ **추천** — "완성된 코드"를 "쓸 수 있는 서비스"로 만드는 최단 경로 |
| C-2. 미구현 기능 보완 우선 | 원계획 S4(결제 Stripe/토스, 관리자 대시보드) 부활 구현 | 수익화 시점이 확정됐을 때 |
| C-3. 콘텐츠/마케팅 우선 | 뉴스 크롤러 정상화(Gemini 유료 또는 OpenAI 전환), 뉴스레터, 소개 슬라이드 공개 | 서비스 본체보다 트래픽 유입이 급할 때 |

**추천 이유**: 코드는 100% "완료"인데 실제로 작동하는지 검증된 적이 없다(배포 0회 성공). 새 기능을 얹기 전에 기존 자산이 실제로 돌아가는 것부터 확인하는 것이 위험이 가장 작고, C-2·C-3 어느 쪽으로 가든 선행 조건이 된다.

### 권장 실행 순서

```
Track A (전부) → Track B1·B3 → C-1 → [PO 결정: C-2 vs C-3] → 잔여 B
```

---

## 부록: 이번 세션 인프라 변경 사항

- C:\ValueLink로 작업 디렉토리 이관 완료 (7,469파일, 검증: C ⊇ G, 빌드 실패는 기존 결함 재현으로 이관 무결 확인)
- 동기화: `scripts/sync-valuelink-to-gdrive.ps1` + 예약 작업 `ValueLink_GDrive_Sync`(로그온+5분, C→G 단방향 미러, node_modules·.next 제외) — 1회 실행 검증 완료 (85파일, 실패 0)
- ⚠️ 앞으로 작업은 반드시 C:\ValueLink에서. G쪽 직접 수정은 다음 미러 때 덮어써짐.
- G쪽 `node_modules`·`.next`(수만 파일)는 미러 제외라 G드라이브에 그대로 남아 용량 점유 중 — 삭제 여부 PO 결정 대기
