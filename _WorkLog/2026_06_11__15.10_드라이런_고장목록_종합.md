# ValueLink 드라이런 전수 점검 — 고장 목록 종합

**점검일**: 2026-06-11
**방법**: 실제 구동 검증 — dev 서버 기동, 전 페이지(40) HTTP 스캔, API GET 8종, 외부 서비스 6종 실연결, 코드 참조 테이블 14종 실DB 대조, Playwright E2E 6종 실브라우저 실행, GitHub Actions API 조회
**원칙**: 비파괴 (DB 쓰기·이메일 발송·배포 트리거 없음)

---

## 🔴 고장 (작동 안 함)

### A. DB-코드 정합 붕괴 — 이번 점검 최대 적발

**근본 원인**: 2026-02-08 DB 사고 후 백업(구 스키마)으로 복원 → v4 스키마가 끝내 재배포되지 않음. 코드는 전부 v4 기준으로 작성됨. "32/32 Task 완료"는 코드 레벨 완료였고 DB 정합은 미복구 상태.

| # | 고장 | 증거 | 죽는 기능 |
|---|------|------|----------|
| 1 | **코드 참조 14개 테이블 중 8개 부재**: `valuation_projects`, `evaluation_requests`, `project_history`, `support_cases`, `partner_referrals`, `deal_news`, `investor_watchlist`, `workflow_approvals` | REST 대조 — PGRST205 | 평가 신청 제출, 프로젝트 생성, 평가 실행 API(`/api/valuation`), 프로젝트 이력, 투자자·파트너·서포터 마이페이지, 워크플로 승인(JP 22개 판단점) |
| 2 | **`valuation_results` 컬럼 불일치** — 실DB: `method, calculation_details(JSONB)` / 코드 기대: `valuation_method, valuation_date, company_name, current_assets...` 등 개별 컬럼 | 실쿼리 42703 에러 | **평가 결과 페이지 5종 영구 404** (어떤 project_id를 줘도) |
| 3 | **`projects` 구 스키마** — `customer_id` 기준 (v4 코드는 `user_id`·`accountant_id` 참조) | 컬럼 덤프 | API 라우트들의 소유권·권한 체크 |

### B. 빌드·배포 계층 (직전 진단에서 확인, 재검증됨)

| # | 고장 | 증거 |
|---|------|------|
| 4 | `next build` 실패 — 타입 오류 269 (Supabase Database 타입 미생성→`never` ~240개 + 깨진 import 1 + tsconfig 과포함) | 직접 빌드 |
| 5 | Vercel 프로덕션 배포 Error — 환경변수 `CRON_SECRET` 앞뒤 공백 | vercel inspect --logs |
| 6 | CI·CD 파이프라인 — 마지막 실행(2/25) 모두 failure | GitHub API |
| 7 | **GitHub Actions 전체 2/27 이후 발화 0회** — 매일 돌아야 할 뉴스 스크래퍼 104일 정지 (워크플로 state는 active로 표시 — 60일 무활동 자동정지 추정, GitHub 웹에서 재활성화 필요) | GitHub API runs 목록 |
| 8 | 공개 URL이 레거시 목업 — GitHub Pages가 1월 정적 프로토타입으로 리다이렉트 | 직접 fetch |

### C. 로컬 인프라

| # | 고장 | 상태 |
|---|------|------|
| 9 | Playwright Chromium 바이너리 부재 (5월 디스크 정리 추정) | ✅ **이번에 설치해 해결** |

---

## ✅ 정상 (실구동 확인)

| 항목 | 결과 |
|------|------|
| dev 서버 | 2.5s 기동, 안정 |
| 페이지 렌더 | 35/40 200 OK (404 5개는 위 #2의 결과 페이지) |
| API 보안 | 8/8 미인증 401 — 인증 가드 정상 작동 |
| 외부 서비스 | **6/6 OK** — Supabase REST·Auth, OpenAI, Anthropic, Gemini(할당량 복구됨), Resend 키 전부 유효 |
| Jest 통합 테스트 | 4/4 통과 (평가 엔진·오케스트레이터) |
| **Playwright E2E** | **6/6 통과** — customer·accountant·admin 실브라우저 로그인+대시보드 (15.6s) |
| E2E 테스트 계정 | 3/3 DB 존재 (customer/accountant/admin) |
| 랜딩 페이지 링크 | href 정상 (/login, /register) — dead link 없음 |

---

## 업그레이드 계획 수정 필요 사항 (Track A 증보)

기존 계획의 A4 "DB 미생성 테이블 4개 생성"은 과소평가였음 → **"DB 스키마 v4 정합 복구"로 확대**:
- 부재 테이블 8개 생성 (schema-v4-final.sql 기준, CREATE만 — 기존 데이터 비파괴)
- `valuation_results`·`projects` 마이그레이션 전략 결정 필요 (갈림길 — PO 승인 대상):
  - (a) 실DB를 v4로 마이그레이션 (기존 25건 결과 데이터 변환 포함) ← 코드 무수정
  - (b) 코드를 실DB 스키마에 맞춤 ← DB 무수정이나 수정 범위 큼
  - (c) v4 신규 테이블 + 구데이터는 보관만 ← 최소 위험, 추천 검토
- GitHub Actions 스케줄 재활성화 (웹 UI에서 워크플로 Enable 또는 빈 커밋 push)

## 점검 산출물
- `_WorkLog/dryrun-external-check.js` — 외부 서비스 6종 점검 (재사용 가능)
- `_WorkLog/dryrun-page-scan.js` — 전 페이지+API 스캔 (재사용 가능)
- `_WorkLog/dryrun-db-table-check.js` — 테이블 정합 대조 (재사용 가능)
